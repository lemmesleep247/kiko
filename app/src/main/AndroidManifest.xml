<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Kiko isn't on the Play Store, so update notifications go through a plain local
         notification instead of Play's push-based update prompts. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Lets Kiko hand a freshly downloaded release APK straight to the system installer instead
         of only being able to point the person at a browser download. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- Only used as a fallback for saving the crash log to the public Downloads folder on
         API 26-28 devices; API 29+ writes there through MediaStore without needing this. -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
    <application
        android:allowBackup="true"
        android:label="Kiko"
        android:theme="@style/Theme.Kiko"
        android:supportsRtl="true"
        android:localeConfig="@xml/locales_config"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules" >
        <activity android:name=".MainActivity" android:exported="true" android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="com.kiko.tracker" android:host="oauth" android:path="/callback" />
            </intent-filter>
            <!-- Lets MAL title links (myanimelist.net/anime/{id}/... or /manga/{id}/...) open directly
                 in Kiko. autoVerify can't be confirmed since Kiko doesn't own myanimelist.net's
                 assetlinks.json, so on some devices the person may need to pick Kiko once from the
                 "Open with" chooser, or set it under Settings > Apps > Kiko > Open by default. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" />
                <data android:scheme="http" />
                <data android:host="myanimelist.net" />
                <data android:host="www.myanimelist.net" />
                <data android:pathPrefix="/anime" />
                <data android:pathPrefix="/manga" />
            </intent-filter>
        </activity>

        <!-- Serves the downloaded update APK (from cacheDir) to the system package installer as a
             content:// URI, since a plain file:// URI is blocked on API 24+. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>